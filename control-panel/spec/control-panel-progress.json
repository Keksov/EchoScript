{
  "initiative": "control-panel",
  "title": "Централизованный UI настроек + управление демонами (Quasar + Bun :3001)",
  "plan": "control-panel-plan.md",
  "branch": "feature/control-panel",
  "baseBranch": "feature/language-routing",
  "created": "2026-07-05",
  "updated": "2026-07-05",
  "conventions": "Зеркалим c:/Games/SoundTrainer: Quasar2(Vite)+Vue3+vue-i18n11+Bun; ui/ собирается в server/public (distDir), hash-routing; один Bun.serve отдаёт public/ (SPA-fallback index.html) + /api/* + /ws; dev quasar dev + devServer.proxy /api,/ws→server; i18n boot ru-default/en-fallback/localStorage; services/api.ts относительный fetch; tabbed SettingsPage.",
  "decisions": [
    "CP-D1: отдельное Bun-приложение control plane :3001 рядом с оркестратором data plane :3000.",
    "CP-D2: config.json = единый источник истины; control-server — ЕДИНСТВЕННЫЙ писатель (atomic+валидация); прочие только читают.",
    "CP-D3: оркестратор hot-reload (fs.watch config) безопасных полей + пометка restart-required + кнопка restart из UI.",
    "CP-D4: UI получает управление демонами — start/stop/restart (портируем monitor_control) + статус (monitor_status: реестр+port/ws-health). ВЛАДЕЛЕЦ 2026-07-05: нативный Monitor НЕ трогаем — работает параллельно (не ретайрим в v1).",
    "CP-D9 (владелец 2026-07-05): auth в v1 не нужен — control-server bind localhost-only, без логина.",
    "CP-D5: выносим ВСЕ настройки (оркестратор+демоны, вкл. VAD-пороги/GPU/DLL/decode); тонкие демон-настройки через CLI-флаги демона, генерируемые лаунчером из config (+ Pascal-правки).",
    "CP-D6: i18n ru/en — у каждой настройки label+description в обоих локалях.",
    "CP-D7: стек Quasar по конвенциям SoundTrainer; ui/→server/public; один порт prod, proxy dev.",
    "CP-D8: провижининг моделей из UI (запуск download_whisper_models.bat + прогресс)."
  ],
  "phases": [
    { "id": "P0", "title": "Plan & ledger", "gate": "pause: обзор плана владельцем (структура/имя/порт/ветка/авторизация)" },
    { "id": "P1", "title": "Скелет control-panel (ui Quasar + server Bun :3001, read-only)", "gate": "pause: dev+build работают, UI отдаётся, виден config + статус демонов" },
    { "id": "P2", "title": "Настройки + оркестратор hot-reload + restart", "gate": "pause: правка config из UI (single writer atomic) + hot-reload + restart-кнопка" },
    { "id": "P3", "title": "Управление демонами (поглощение Monitor)", "gate": "pause: start/stop/restart + live-статус, паритет с Monitor" },
    { "id": "P4", "title": "Демон-настройки (CLI-флаги VAD/decode/GPU + лаунчер-из-config)", "gate": "pause: правка VAD/decode из UI доезжает до демона, эффект подтверждён" },
    { "id": "P5", "title": "Провижининг моделей + полнота i18n + docs", "gate": "pause: скачивание из UI, ru/en полны, тесты зелёные, docs" }
  ],
  "steps": [
    {
      "id": "CP0.1",
      "phase": "P0",
      "title": "Plan & ledger",
      "status": "done",
      "dependsOn": [],
      "artifacts": [
        "control-panel/spec/control-panel-plan.md",
        "control-panel/spec/control-panel-progress.json"
      ],
      "verify": "план фиксирует решения CP-D1..CP-D8, конвенции SoundTrainer, инвентарь настроек и фазы P0-P5; JSON парсится; DAG ацикличен",
      "evidence": "Изучены конвенции SoundTrainer (ui/ Quasar2+Vite+Bun → server/public; Bun.serve public+/api+/ws; i18n boot ru/en; services/api.ts относительный fetch; tabbed SettingsPage). Ветка feature/control-panel от feature/language-routing (актуальный config.json + VAD-демон)."
    },
    {
      "id": "CP1.1",
      "phase": "P1",
      "title": "Скелет control-panel (ui + server :3001, read-only)",
      "status": "done",
      "dependsOn": ["CP0.1"],
      "artifacts": [
        "control-panel/ui/quasar.config.ts",
        "control-panel/ui/package.json",
        "control-panel/ui/src/boot/i18n.ts",
        "control-panel/ui/src/i18n/locales/ru.json",
        "control-panel/ui/src/i18n/locales/en.json",
        "control-panel/ui/src/services/api.ts",
        "control-panel/ui/src/pages/SettingsPage.vue",
        "control-panel/server/server.ts",
        "control-panel/server/config.ts"
      ],
      "verify": "quasar dev (proxy /api,/ws→:3001) и quasar build (→server/public) работают; Bun-сервер :3001 отдаёт UI + GET /api/health,/api/config (read),/api/daemons (статус); UI показывает read-only config и статус демонов; i18n-каркас ru/en; vue-tsc зелёный",
      "evidence": "Скелет по конвенциям SoundTrainer. server/ (Bun :3001, localhost-only): config.ts (порт/пути), server.ts (Bun.serve serveStatic SPA-fallback + /api/health,/api/config GET,/api/daemons GET), status.ts (merge config.ws_daemons + jobs/registry по TTL: fresh/stale/down/orphan) + status.test.ts (5 pass). ui/ (Quasar2 Vite + Vue3 + vue-i18n): quasar.config (distDir→server/public, hash, boot i18n, devServer.proxy /api,/ws→:3001), MainLayout (title + RU/EN тумблер), OverviewPage (табы Демоны/Конфиг, read-only), services/api.ts относительный fetch, i18n locales ru/en. ПРОВЕРЕНО: bun install (server+ui) ок; vue-tsc + tsc чисты; quasar build → server/public успешно; сервер отдаёт / (index.html), /assets/*.js 200, SPA-fallback /nope 200; /api/health ok; /api/daemons — оба демона fresh=True; /api/config — весь конфиг. (curl через HTTP_PROXY даёт 502 — обходится --noproxy; браузер/quasar-proxy бьют напрямую.) ПРАВКА по фидбэку владельца: в список сервисов добавлен САМ ОРКЕСТРАТОР (он не ws-демон, в реестр не пишет) — статус через TCP-пробу порта :3000 (isPortOpen, proxy-immune); status.ts получил kind (orchestrator|ws-daemon) + up + detail; UI показывает тип иконкой, /api/daemons отдаёт [orchestrator, ...daemons]. Тесты 6/6, оркестратор в списке up=listening."
    },
    {
      "id": "CP2.1",
      "phase": "P2",
      "title": "Настройки + оркестратор hot-reload + restart",
      "status": "done",
      "dependsOn": ["CP1.1"],
      "artifacts": [
        "control-panel/server/settings-schema.ts",
        "control-panel/server/settings.ts",
        "control-panel/server/orchestrator-control.ts",
        "control-panel/server/server.ts",
        "control-panel/ui/src/components/SettingsForm.vue",
        "control-panel/ui/src/i18n/locales/ru.json",
        "control-panel/ui/src/i18n/locales/en.json",
        "orchestrator/src/config.ts",
        "orchestrator/src/index.ts",
        "orchestrator/src/scheduler.ts",
        "orchestrator/src/daemon-registry.ts"
      ],
      "verify": "GET/PUT /api/config (single writer, atomic temp+rename, валидация по схеме); формы оркестратор + ws_daemons с i18n label/описанием; оркестратор fs.watch(config.json) перечитывает безопасные поля (интервалы/TTL/drop_stable/requeue/роутинг), помечает restart-required; кнопка restart оркестратора работает; bun test",
      "evidence": "СЕРВЕР: settings-schema.ts (поля + reload-класс hot|restart), settings.ts (validate чисто + shallow/ws merge + atomic temp+rename + changedReloadClasses), эндпоинты GET/PUT /api/config, GET /api/schema, POST /api/orchestrator/restart. ОРКЕСТРАТОР hot-reload: config.ts watchConfigForReload (fs.watch+debounce, битый reload пропускается), index.ts мутирует общий config на месте (исключая restart-класс jobsRoot/maxWorkers), daemonRegistry.setTtlMs, scheduler.applyConfigReload (сброс таймера+watcher'ов). UI: SettingsForm.vue (формы из схемы, i18n label+описание на каждое поле ru/en, бейдж hot/restart, save→PUT, баннер+кнопка restart), вкладка Настройки в OverviewPage. ЖИВЫЕ ТЕСТЫ: PUT poll_interval_ms=600 → оркестратор логнул 'config hot-reloaded'; PUT max_workers=2 → restartRequired=true; битый PUT → 400 конфиг цел; POST restart → ok=true, оркестратор сменил PID, :3001 не унаследован (детач-spawn+stdio ignore, idleTimeout 60). Тесты: control-panel 13, orchestrator 64; tsc+vue-tsc+build чисты. ФИКСЫ по ходу: Bun.serve idleTimeout=60 (рестарт >10с); restart через detached+stdio:ignore+port-probe (иначе spawn наследовал слушающий сокет :3001)."
    },
    {
      "id": "CP3.1",
      "phase": "P3",
      "title": "Управление сервисами (start/stop/restart)",
      "status": "done",
      "dependsOn": ["CP1.1"],
      "artifacts": [
        "control-panel/server/services.json",
        "control-panel/server/services-control.ts",
        "control-panel/server/server.ts",
        "control-panel/ui/src/pages/OverviewPage.vue"
      ],
      "verify": "start/stop/restart сервиса из UI (по инвентарю services.json + скрипты) + live-статус (реестр jobs/registry + port-probe); паритет с нативным Monitor (который НЕ трогаем); bun test",
      "evidence": "services.json (инвентарь lifecycle: orchestrator + whisperdaemon + whisperdaemon_en → start/stop-скрипты; host/port/язык — из config.json). services-control.ts: controlService(name, start|stop|restart) обобщает оркестраторный рестарт на любой сервис — detached spawn + stdio:ignore (без утечки сокета) + верификация port-probe (для демонов таймаут 60с под warmup, стоп 10с). server.ts: обобщённый POST /api/services/:name/:action + /api/orchestrator/restart через controlService; /api/daemons отдаёт флаг controllable. UI: колонка «Действия» в таблице Сервисы — кнопки старт/стоп/рестарт на строку с busy-спиннером, i18n ru/en; после действия — авто-reload. Старый orchestrator-control.ts удалён (свёрнут в services-control). ЖИВОЙ ТЕСТ: POST /api/services/whisperdaemon_en/restart → ok=true, EN-демон сменил PID 45224→40312 и снова detail=ready (прогрелся); control-panel :3001 не унаследован. Все 3 сервиса controllable=True. tsc+vue-tsc+build чисты."
    },
    {
      "id": "CP4.1",
      "phase": "P4",
      "title": "Демон-настройки (env из config при старте)",
      "status": "done",
      "dependsOn": ["CP2.1", "CP3.1"],
      "artifacts": [
        "services/whisperdaemon/app/src/WhisperDaemon.pas",
        "control-panel/server/settings-schema.ts",
        "control-panel/server/settings.ts",
        "control-panel/server/services-control.ts",
        "control-panel/ui/src/components/SettingsForm.vue"
      ],
      "verify": "в WhisperDaemon.pas VAD-пороги/decode читаются из env (дефолты сохранены); лаунчер control-server выставляет env из config при старте демона; формы UI (bool/float + i18n); пересборка демона; эффект подтверждён",
      "evidence": "Подход env-из-config (не CLI-парсинг — меньше Pascal). WhisperDaemon.pas: whisperEnvFloat/Int (float с '.'-разделителем, §7 PASCAL_RULES); VAD threshold/speech_pad/min-speech/min-silence + noSpeechThold/entropyThold из env с текущими дефолтами; лог 'inference vad=.. vad_thold=.. no_speech=..'. Пересборка ок. Схема: типы float/bool + target(orchestrator|daemon) + default; WS_DAEMON_FIELDS += vad/vad_threshold/vad_speech_pad_ms/no_speech_thold/entropy_thold/gpu/gpu_device (target daemon, reload restart); DAEMON_ENV_MAP config→env. settings.ts: валидация float/bool; changedReloadClasses по target (демон-поля не триггерят restart оркестратора). services-control: daemonEnvFromConfig → env при start/restart (start-скрипт+Start-Process наследуют). UI: q-toggle(bool)/число(float) + бейджи hot/restart + двуязычные пояснения + note про рестарт демона. ЖИВОЙ ТЕСТ: PUT whisperdaemon_en.vad_threshold=0.6 → POST start через панель → демон залогировал vad_thold=0,60 (env дошёл). restartRequired=False (демон-поле). Тесты 14; tsc+vue-tsc+build чисты."
    },
    {
      "id": "CP5.1",
      "phase": "P5",
      "title": "Провижининг моделей + полнота i18n + docs",
      "status": "todo",
      "dependsOn": ["CP2.1", "CP3.1", "CP4.1"],
      "artifacts": [
        "control-panel/server/models.ts",
        "control-panel/ui/src/pages/ModelsPage.vue",
        "EchoRecorder/docs/ARCHITECTURE.md"
      ],
      "verify": "скачивание модели из UI (запуск download_whisper_models.bat + прогресс/лог); каждая настройка двуязычна ru/en (label+описание, проверка полноты локалей); bun test (control-server) + vue-tsc зелёные; ARCHITECTURE.md описывает control-plane :3001",
      "evidence": ""
    }
  ]
}
