[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "echoscript-shared"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
	"watchdog",
]

[tool.setuptools.packages.find]
include = ["echoscript_shared*"]
