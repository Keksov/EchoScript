{
  "name": "echoscript-orchestrator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ECHOSCRIPT_DEV=1 bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
