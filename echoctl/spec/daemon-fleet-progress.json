{
  "initiative": "daemon-fleet",
  "title": "Динамический флот демонов: разные модели на разных портах + удаление моделей; движок — FPC CLI echoctl, UI/Bun — обёртка",
  "plan": "daemon-fleet-plan.md",
  "branch": "feature/daemon-fleet",
  "baseBranch": "feature/control-panel",
  "created": "2026-07-06",
  "updated": "2026-07-06",
  "conventions": "FPC CLI по лекалу EchoRecorder/cli: program {$mode objfpc}{$H+}, ручной парсинг аргументов, scripts/{build,run,smoke,clean}_x64.bat; каждая команда с --json для UI/сервера; config.json — atomic temp+rename, read-modify-write с сохранением неизвестных ключей; PASCAL_RULES соблюдаем.",
  "decisions": [
    "DF-D1 (big-bang): echoctl — единственный движок управления и единственный писатель config.json; TS-логика (settings/services-control/models-provision) переносится в CLI; Bun control-server → чистый прокси.",
    "DF-D2 (JSON-контракт): каждая команда поддерживает --json (машинный вывод); человекочитаемый по умолчанию.",
    "DF-D3 (охват v1): whisper + vosk (ws-демоны); python-модели config.models (gemma/vibevoice/borealis) вне v1.",
    "DF-D4 (удаление): по умолчанию отказ, если модель ссылается инстанс (показать кто); --force каскадит инстансы; всегда --dry-run preview → подтверждение в UI.",
    "DF-D5 (per-instance настройки): запись ws_daemons расширяется полем settings{} (зеркало DAEMON_ENV_MAP); лаунчер генерит env/флаги из settings.",
    "DF-D6 (порты): авто-аллокация per-engine (whisper 78xx, vosk 77xx) + ручной override; валидация коллизий.",
    "DF-D7 (spawn + фикс сокета): демоны через CreateProcess(bInheritHandles=FALSE) → детач + устранение утечки слушающего сокета :3001.",
    "DF-D8 (vosk generic): vosk-демон — резолвинг путь-или-имя + VOSK_MODELS_ROOT, back-compat со старыми именами.",
    "DF-D9: config.json — единый источник истины (как CP-D2), единственный писатель теперь echoctl; оркестратор по-прежнему hot-reload'ит ws_daemons."
  ],
  "phases": [
    { "id": "P0", "title": "План + скелет echoctl + config I/O", "gate": "pause: обзор плана владельцем; build зелёный; daemons list --json читает ws_daemons" },
    { "id": "P1", "title": "CRUD инстансов + per-instance settings + аллокация портов", "gate": "pause: add/remove/edit атомарно пишут config; валидация; unit-смоки" },
    { "id": "P2", "title": "Жизненный цикл моделей (manifest/list/download/delete)", "gate": "pause: список/скачивание/удаление из CLI; safety (refuse/force/dry-run) подтверждён" },
    { "id": "P3", "title": "Обобщение демонов (vosk generic; whisper + settings pass-through)", "gate": "pause: произвольная модель движка на произвольном порту стартует прямым запуском exe" },
    { "id": "P4", "title": "spawn/stop в echoctl (no-inherit) + config get/set/schema", "gate": "pause: start/stop/restart из CLI с ожиданием реального warmup + port-verify; утечка :3001 устранена" },
    { "id": "P5", "title": "Bun → прокси + UI (create/delete/settings/model-delete) + i18n", "gate": "pause: UI = обёртка; мутации через echoctl; ru/en полны; тесты зелёные" },
    { "id": "P6", "title": "Оркестратор dynamic pickup + E2E", "gate": "pause: job на новой модели/порту проходит; удаление модели refuse→force; docs" }
  ],
  "steps": [
    {
      "id": "DF0.1",
      "phase": "P0",
      "title": "План и леджер",
      "status": "done",
      "dependsOn": [],
      "artifacts": ["echoctl/spec/daemon-fleet-plan.md", "echoctl/spec/daemon-fleet-progress.json"],
      "verify": "план описывает архитектуру (echoctl-движок, Bun-прокси), командный интерфейс, модель данных, фазы; решения DF-D1..D9 зафиксированы; DAG ацикличен (python-проверка)",
      "evidence": "Создан после обсуждения с владельцем 2026-07-06. Развилки: big-bang (DF-D1), whisper+vosk (DF-D3), удаление refuse+--force+dry-run (DF-D4). Фундамент: ws_daemons уже keyed-инстансы с host/port/engine/language/model_name; whisper-демон уже generic (resolveWhisperModelPath путь-или-имя+WHISPER_MODELS_ROOT); vosk хардкодит 3 имени; CLI-паттерн EchoRecorder/cli."
    },
    {
      "id": "DF0.2",
      "phase": "P0",
      "title": "Скелет echoctl (program + scripts + диспетчер аргументов)",
      "status": "done",
      "dependsOn": ["DF0.1"],
      "artifacts": ["echoctl/src/echoctl.pas", "echoctl/scripts/fpc-x64.cfg", "echoctl/scripts/build_x64.bat", "echoctl/scripts/run_x64.bat", "echoctl/scripts/smoke_x64.bat", "echoctl/scripts/clean.bat"],
      "verify": "build_x64.bat собирает без ошибок; echoctl version и echoctl help работают; диспетчер команд daemons/config/models с заглушками",
      "evidence": "FPC-тулчейн EchoRecorder/VendorsCore/fpc (образец orchestrator/monitor/cli; top-level cfg с путём ..\\EchoRecorder\\VendorsCore). echoctl.pas: {$mode objfpc}{$H+}, ручной разбор аргументов, диспетчер daemons/models/config + version/help, коды возврата 0/1/2/3 (OK/runtime/usage/notimpl), DecimalSeparator='.' (PASCAL_RULES §7). Скрипты build/run/smoke/clean_x64 (CRLF). smoke зелёный: build→echoctl.exe; version→'echoctl 0.1.0' exit0; help/no-args→usage exit0; unknown group→exit!=0; 'daemons list'→not implemented exit3. build/ в .gitignore."
    },
    {
      "id": "DF0.3",
      "phase": "P0",
      "title": "Модуль config I/O (fpjson load + atomic temp+rename write, сохранение неизвестных ключей)",
      "status": "done",
      "dependsOn": ["DF0.2"],
      "artifacts": ["echoctl/src/echoctl_config.pas", "echoctl/tests/test_config.pas", "echoctl/scripts/test_x64.bat"],
      "verify": "round-trip config.json сохраняет ВСЕ ключи (speech/models/ws_daemons/…); запись атомарна (temp+rename); smoke: load→save без семантических потерь",
      "evidence": "echoctl_config.pas: loadConfigObject (fpjson GetJSON → TJSONObject, владелец вызывающий), saveConfigAtomic (FormatJSON → temp + Windows.MoveFileEx MOVEFILE_REPLACE_EXISTING — истинная атомарная замена на NTFS), resolveDefaultConfigPath (поиск config.json вверх от exe + фолбэк CWD). DeleteFile квалифицирован SysUtils.DeleteFile (Windows-юнит тоже экспортирует). Тест test_config.pas (PASCAL_RULES §8, Ok/ExitCode): load→saveAtomic→reload — 9/9 passed: топ-уровень count+names, ws_daemons count+names, jobs_root/max_workers сохранены. test_x64.bat собирает и гоняет тест (exit 0)."
    },
    {
      "id": "DF0.4",
      "phase": "P0",
      "title": "daemons list [--json] (чтение ws_daemons → JSON)",
      "status": "done",
      "dependsOn": ["DF0.3"],
      "artifacts": ["echoctl/src/echoctl_daemons.pas", "echoctl/src/echoctl.pas"],
      "verify": "echoctl daemons list --json выводит оба текущих инстанса с host/port/engine/language/model_name; человекочитаемый режим — таблица",
      "evidence": "echoctl_daemons.runDaemonsList: JSON-массив (name/engine/language/host/port/model_name/settings, settings клонируется или пустой объект) или таблица NAME/ENGINE/LANG/ENDPOINT/MODEL. CLI: добавлены hasFlag/optionValue/loadActiveConfig (--config override + resolveDefaultConfigPath), dispatchDaemons.list→doDaemonsList, остальные daemons-команды всё ещё заглушки. Живой вывод: оба инстанса whisperdaemon(:7801 ru podlodka)/whisperdaemon_en(:7802 en turbo). smoke зелёный: list→exit0, list --json содержит инстансы, daemons add→exit3 (заглушка)."
    },
    {
      "id": "DF1.1",
      "phase": "P1",
      "title": "daemons add (валидация + append + atomic write + --json)",
      "status": "done",
      "dependsOn": ["DF0.4"],
      "artifacts": ["echoctl/src/echoctl_daemons.pas", "echoctl/src/echoctl_common.pas", "echoctl/src/echoctl.pas", "echoctl/scripts/smoke_crud_x64.bat"],
      "verify": "add --engine --model --port создаёт запись ws_daemons; отвергает неизвестный engine/модель, занятый порт, дублирующее имя; config записан атомарно",
      "evidence": "Введён echoctl_common (коды 0/1/2/3 + writeErr/fail). Daemons-команды рефакторнуты на путь: сами грузят config и (мутирующие) сохраняют атомарно. runDaemonsAdd: валидация engine∈{whisper,vosk}, model∈config.models (DF2.1 расширит манифестом), port 1..65535 + не занят, имя уникально (дефолт engine_lang); при успехе append в ws_daemons + saveConfigAtomic + вывод (json/human). Имя по умолчанию из engine+lang. smoke_crud_x64 на КОПИИ config (build/x64/test-config.json): add OK ('added daemon test_whisper …') + инстанс в list; отвергнуты дубль-имя/коллизия-порта/неизвестный engine/неизвестная модель/битый порт/без engine. Рабочий config.json не тронут (2 инстанса). base smoke: stub-проверка переведена на 'config schema'→exit3."
    },
    {
      "id": "DF1.2",
      "phase": "P1",
      "title": "daemons remove <name>",
      "status": "done",
      "dependsOn": ["DF1.1"],
      "artifacts": ["echoctl/src/echoctl_daemons.pas", "echoctl/src/echoctl.pas"],
      "verify": "remove удаляет запись ws_daemons атомарно; отвергает несуществующее имя; --json результат",
      "evidence": "runDaemonsRemove: IndexOfName→Delete(idx)+saveConfigAtomic; отсутствующее имя→fail('no such instance'); --json → {\"removed\":\"<name>\"}. CLI: хелпер targetName (позиционный аргумент после команды или --name), doDaemonsRemove, dispatch remove→реальный (edit/start/stop/restart ещё заглушки). smoke_crud расширен: remove test_whisper OK, инстанс исчез из list, remove nope→отказ. base smoke OK."
    },
    {
      "id": "DF1.3",
      "phase": "P1",
      "title": "per-instance settings{} + daemons edit --set k=v + движко-схема",
      "status": "done",
      "dependsOn": ["DF1.1"],
      "artifacts": ["echoctl/src/echoctl_daemons.pas", "echoctl/src/echoctl_schema.pas", "echoctl/src/echoctl.pas"],
      "verify": "edit --set vad_threshold=0.4 пишет settings в запись инстанса; схема per-engine (whisper: vad/vad_threshold/gpu/no_speech_thold/…, зеркало DAEMON_ENV_MAP); валидация типов/диапазонов",
      "evidence": "echoctl_schema: TSettingSpec (bool/int/float + min/max), findSettingSpec (whisper: vad, vad_threshold[0..1], vad_speech_pad_ms[0..5000], no_speech_thold[0..1], entropy_thold[>=0], gpu, gpu_device[0..16]; vosk — нет в v1), parseSettingValue (в TJSONBoolean/Integer/Float, float с fs.DecimalSeparator='.'). runDaemonsEdit: --model (modelKnown), --port (диапазон+коллизия excl. self, in-place через .Integers), несколько --set key=value (applySet: findSettingSpec+parse+settings.set), 'nothing to edit' если пусто; in-place .Strings сохраняет порядок ключей. CLI: collectSets (все --set), doDaemonsEdit, Types.TStringDynArray. smoke_crud +7 проверок edit — все ОК. Проверка типов записи: vad→bool, vad_threshold→0.35(float), gpu_device→1(int)."
    },
    {
      "id": "DF1.4",
      "phase": "P1",
      "title": "Авто-аллокация портов per-engine + override + проверка коллизий",
      "status": "done",
      "dependsOn": ["DF1.1"],
      "artifacts": ["echoctl/src/echoctl_daemons.pas", "echoctl/scripts/smoke_crud_x64.bat"],
      "verify": "add без --port выдаёт свободный порт из диапазона движка (whisper 78xx, vosk 77xx); коллизия порта между инстансами отвергается",
      "evidence": "enginePortRange (whisper 7801-7899, vosk 7701-7799) + allocatePort (первый свободный, -1 при исчерпании). runDaemonsAdd: если --port не задан (<=0) → авто-аллокация из диапазона движка, иначе валидация диапазона+коллизии. smoke: add whisper без порта → auto_w на 7803 (7801/7802 заняты whisperdaemon/_en), add vosk без порта → auto_v на 7701. Коллизия/дубли по-прежнему отвергаются. base+crud+roundtrip зелёные."
    },
    {
      "id": "DF2.1",
      "phase": "P2",
      "title": "models-manifest.json + models list [--json]",
      "status": "done",
      "dependsOn": ["DF0.3"],
      "artifacts": ["echoctl/models-manifest.json", "echoctl/src/echoctl_models.pas", "echoctl/src/echoctl_config.pas", "echoctl/src/echoctl.pas", "echoctl/scripts/smoke_models_x64.bat"],
      "verify": "манифест портирован из models-provision.ts (whisper podlodka/en/large/turbo, vad, diarization); list показывает downloaded-state/размеры/пути; vosk-модели (C:\\var\\vosk) помечены как внешние read-only",
      "evidence": "models-manifest.json портирован из models-provision.ts: podlodka(no-dl, note conversion), en/vad/diarization(download bat), + добавлены vosk_ru/vosk_ru_cmd/vosk_en как external_dir (C:/var/vosk, read-only), у каждого model_name для связи с ws_daemons (DF2.3). echoctl_config: findFileUpwards (обобщённый поиск от exe) + resolveRepoRoot. echoctl_models: computeInfo (file-based: FindFirst размер, downloaded=все файлы есть; external_dir: DirectoryExists, size null), stateText downloaded/missing/absent, JSON {id,model,kind,model_name,downloadable,downloaded,size_mb,note,paths} + таблица. Живо: podlodka/en 1549.3MB, vad 0.8MB, diarization 51MB — downloaded; vosk — external downloaded size null. Размеры сверены с ls (podlodka==en==1624555275, оба turbo-база). smoke_models зелёный."
    },
    {
      "id": "DF2.2",
      "phase": "P2",
      "title": "models download <id>",
      "status": "done",
      "dependsOn": ["DF2.1"],
      "artifacts": ["echoctl/src/echoctl_models.pas", "echoctl/src/echoctl.pas", "echoctl/scripts/smoke_models_x64.bat"],
      "verify": "download <id> запускает download-скрипт из манифеста; идемпотентно (скачанное пропускается); --json со статусом старта",
      "evidence": "runModelsDownload: находит запись, отвергает external_dir (vosk) и no-download (podlodka), unknown id; идемпотентность — короткое замыкание: если entryDownloaded → status 'skipped' без запуска. Иначе строит 'cmd /c \"<repoRoot+script>\" <args>' (+ '1>&2' в --json, чтобы stdout остался чистым JSON), cwd=repoRoot, ExecuteProcess, по exit-коду completed/failed + перепроверка downloaded. Проверено: download en --json → skipped(exit0, без сети), download podlodka → отказ (staged via conversion), vosk_ru → отказ (external), bogus → unknown id. smoke_models: 3 reject-пути (podlodka/vosk_ru/bogus). Реальная сетевая загрузка в смоуке не гоняется намеренно; путь ExecuteProcess срабатывает для реально отсутствующей скачиваемой модели."
    },
    {
      "id": "DF2.3",
      "phase": "P2",
      "title": "models delete <id> (--dry-run / --force, refuse-if-referenced + каскад)",
      "status": "done",
      "dependsOn": ["DF2.1", "DF1.2"],
      "artifacts": ["echoctl/src/echoctl_models.pas", "echoctl/src/echoctl.pas", "echoctl/scripts/smoke_delete_x64.bat", "echoctl/tests/fixtures/delete-manifest.json", "echoctl/tests/fixtures/delete-config.json"],
      "verify": "delete без --force отвергает, если инстанс ссылается (показывает кто); --dry-run печатает файлы+конфиг-записи к удалению; --force удаляет файлы + каскадит инстансы; внешние vosk-каталоги — с предупреждением",
      "evidence": "runModelsDelete: собирает существующие файлы (file-based) + ссылающиеся инстансы (ws_daemons.model_name==entry.model_name). --dry-run: превью (files+external_dir+referencing_instances+would_cascade), 0 изменений. Блоки без --force: referenced → отказ (перечисляет кто); external → отказ (каталог не трогается). --force: DeleteFile файлов + каскад-удаление инстансов + saveConfigAtomic; внешний каталог НЕ удаляется (предупреждение 'left in place'). Добавлен override --manifest (+ рефактор runModelsList/Download на путь манифеста от CLI) для безопасных тестов. smoke_delete на ФЕЙКОВОЙ модели (fixtures/delete-*.json + build/x64/fake-model.bin): refuse→файл цел; dry-run --force→файл цел; --force→файл удалён + fake_inst убран из конфига; bogus→отказ. smoke_models: delete en --dry-run (боевой манифест, без удаления) + bogus. Регрессий нет (base/crud/roundtrip зелёные)."
    },
    {
      "id": "DF3.1",
      "phase": "P3",
      "title": "voskdaemon: generic-резолвинг модели (путь-или-имя + VOSK_MODELS_ROOT, back-compat)",
      "status": "done",
      "dependsOn": ["DF0.2"],
      "artifacts": ["services/voskdaemon/app/src/VoskDaemon.pas"],
      "verify": "прямой запуск VoskDaemon.exe с произвольной моделью (путь ИЛИ имя-каталога под VOSK_MODELS_ROOT) на произвольном порту → warmup ready + describe; старые имена vosk_ru/vosk_ru_cmd/vosk_en работают",
      "evidence": "resolveVoskModelName: back-compat vosk_ru/vosk_ru_cmd/vosk_en→каталоги, иначе имя как есть (каталог под root). resolveVoskModelPath: если DirectoryExists(name) → абсолютный путь как есть, иначе root+name. resolveVoskDllPath (DLL модель-независим): VOSK_DLL_PATH override → venv одноимённого сервиса (back-compat) → fallback venv vosk_ru. Usage-строка обновлена. Пересобрано. ПРОВЕРЕНО (малая модель, 3 инстанса одновременно): generic-имя vosk-model-small-ru-0.22→:7788 warmup ready (загрузка из C:\\var\\vosk\\...); абсолютный путь C:\\var\\vosk\\vosk-model-small-ru-0.22→:7789 warmup ready; back-compat vosk_ru_cmd→:7787 warmup ready; describe_ack {model:vosk-model-small-ru-0.22, port:7788}. Разные модели на разных портах — работает."
    },
    {
      "id": "DF3.2",
      "phase": "P3",
      "title": "whisper: подтвердить generic + settings→env pass-through (2-я модель на 2-м порту)",
      "status": "done",
      "dependsOn": ["DF0.2"],
      "artifacts": ["services/whisperdaemon/app/src/WhisperDaemon.pas"],
      "verify": "прямой запуск WhisperDaemon.exe со 2-й скачанной моделью на 2-м порту + env из settings (WHISPER_VAD_THRESHOLD и т.п.) → лог inference показывает применённые настройки; при необходимости мелкие правки",
      "evidence": "ПОДТВЕРЖДЕНО generic: resolveWhisperModelPath уже принимает путь / .bin / имя→ggml-<name>.bin под WHISPER_MODELS_ROOT. ПОДТВЕРЖДЕНО settings→env: демон читает WHISPER_VAD_THRESHOLD/NO_SPEECH_THOLD/ENTROPY_THOLD/VAD_SPEECH_PAD_MS (строки ~1544) при инференсе. Правка: добавлен echo эффективных настроек на старте (после 'warmup ready') — наблюдаемость + дешёвая проверка без аудио; + fmtFloat2 (FormatFloat с fs.DecimalSeparator='.', §7) — иначе лог выдавал '0,55'. ПРОВЕРЕНО: запуск whisper_en_turbo на :7810 с env VAD_THRESHOLD=0.55/NO_SPEECH=0.7/ENTROPY=2.8/PAD=250 → загрузка ggml-whisper_en_turbo.bin (generic) + 'effective settings: vad=True vad_thold=0.55 vad_speech_pad_ms=250 no_speech_thold=0.70 entropy_thold=2.80' (точки, все значения применены). Пересобрано."
    },
    {
      "id": "DF4.1",
      "phase": "P4",
      "title": "daemons start/stop/restart через CreateProcess(bInheritHandles=FALSE) + ожидание warmup + port-verify",
      "status": "done",
      "dependsOn": ["DF1.1", "DF3.1"],
      "artifacts": ["echoctl/src/echoctl_launch.pas", "echoctl/src/echoctl_daemons.pas", "echoctl/src/echoctl.pas", "echoctl/scripts/smoke_lifecycle_x64.bat"],
      "verify": "start поднимает демон детачем, ждёт РЕАЛЬНОГО warmup-ready (не только открытия порта) с таймаутом; stop/restart работают; демон НЕ наследует хендлы (проверка: рестарт панели/родителя не блокируется удержанием :3001)",
      "evidence": "echoctl_launch: spawnDaemon через CreateProcess(cmd.exe /c \"\"exe\" args > log 2>&1\", bInheritHandles=FALSE, CREATE_NO_WINDOW) — редирект делает cmd (открывает лог сам), поэтому наследование хендлов НЕ нужно, ставим FALSE → демон не наследует ни один хендл echoctl (вкл. :3001 от Bun) = фикс DF-D7. waitWarmup поллит лог на 'warmup ready'/'warmup failed'/timeout (реальный прогрев). isPortOpen/waitPortClosed через ssockets. FPC-версия Windows-юнита без STARTUPINFOEX, поэтому whitelist-хендлов недоступен — cmd-редирект был единственным чистым путём совместить логи и no-inherit. echoctl_daemons: daemonExePath (engine→exe), buildDaemonEnv (whisper WHISPER_MODELS_ROOT + settings→WHISPER_VAD/THRESHOLD/… ; vosk дефолт), buildDaemonArgs (--model-name/host/port + --gpu/--gpu-device), stopDaemonByPort (powershell Get-NetTCPConnection LocalPort→Stop-Process), start/stop/restart. CLI: --timeout (180с деф). ПРОВЕРЕНО на vosk_ru_cmd (:7786/:7785): start→'started' после реального warmup, повторный start→'already-running', describe_ack {model:vosk_ru_cmd}, restart=stop+start, stop→порт down; start/stop nope→exit2. smoke_lifecycle (guarded happy-path + error-пути) зелёный; base/crud/models/delete без регрессий. E2E-проверка фикса :3001 (Bun→echoctl→демон) — в P6 через панель."
    },
    {
      "id": "DF4.2",
      "phase": "P4",
      "title": "config get/set/schema (перенос settings.ts: validate/merge/atomic write)",
      "status": "done",
      "dependsOn": ["DF0.3"],
      "artifacts": ["echoctl/src/echoctl_schema.pas", "echoctl/src/echoctl.pas", "echoctl/scripts/smoke_x64.bat"],
      "verify": "config get --json = текущие настройки оркестратора; config set валидирует (тип/диапазон/reload-класс) и атомарно пишет; config schema --json = поля с hot/restart и picker",
      "evidence": "echoctl_schema: TOrchField + orchField (10 полей, порт ORCHESTRATOR_FIELDS: max_workers int restart 1-64, poll/stream/ttl/drop/requeue int hot, default_model select optionsFrom models, ffmpeg_path picker file, jobs_root picker directory restart). runConfigGet (значения из config.json, int/string), runConfigSet (findOrchField→валидация типа/диапазона→in-place .Integers/.Strings сохраняет порядок→saveConfigAtomic; вывод с reload-классом hot/restart), runConfigSchema (key/type/reload/min/max/picker/optionsFrom). CLI dispatchConfig get/set/schema; убран мёртвый notImplemented/isKnown (заглушек не осталось), базовый smoke переведён на 'config bogus'→exit2. ПРОВЕРЕНО: schema=10 полей (restart: max_workers/jobs_root); get max_workers=1; set max_workers 8→'(reload: restart)' записан как int 8; set poll_interval_ms 500→'(reload: hot)'; set max_workers 999→above max 64 exit2; set bogus→unknown exit2. base/crud/models/delete зелёные."
    },
    {
      "id": "DF5.1",
      "phase": "P5",
      "title": "Bun control-server → прокси к echoctl (демонтаж TS-мутаций)",
      "status": "done",
      "dependsOn": ["DF4.1", "DF4.2", "DF2.2", "DF2.3", "DF1.2", "DF1.3"],
      "artifacts": ["control-panel/server/server.ts", "control-panel/server/echoctl.ts", "control-panel/server/echoctl.test.ts"],
      "verify": "routes (PUT config, POST/DELETE daemons, models download/delete, services action) вызывают echoctl --json и прокидывают результат; settings.ts/services-control.ts/models-provision.ts удалены/сведены к шимам; server-тесты зелёные",
      "evidence": "echoctl.ts: runEchoctl/runEchoctlJson (Bun.spawn echoctl.exe) + echoctlDaemonNames. server.ts переведён на прокси: GET /api/models → echoctl models list (map size_mb→sizeMb); POST /api/models/:id/download → валидация через list + fire-and-forget echoctl download (async, UI поллит); DELETE /api/models/:id (?force/?dryRun) → echoctl models delete (НОВОЕ); POST /api/daemons (add), DELETE /api/daemons/:name (remove), PATCH /api/daemons/:name (edit) → echoctl (НОВОЕ); GET /api/schema orchestrator → echoctl config schema; POST /api/services/:name/:action диспетчеризуется: name∈ws_daemons → echoctl daemons <action> (socket-safe), иначе controlService (оркестратор/vosk-inventory). models-provision.ts УДАЛЁН. ГРАНИЦА (осознанно): PUT /api/config (bulk settings через applyConfig) и services-control (оркестратор+streaming-vosk, они НЕ ws_daemons) сохранены — bulk-форма мигрирует на гранулярные echoctl-вызовы в DF5.2 с редизайном UI. Тесты: +echoctl.test.ts (реальный shell-out: daemons list→whisperdaemon, models list→en/vosk_ru, config schema reload-классы). typecheck чист; 18/18 зелёных."
    },
    {
      "id": "DF5.2",
      "phase": "P5",
      "title": "UI: создание/удаление инстансов + per-instance настройки + удаление моделей + i18n",
      "status": "done",
      "dependsOn": ["DF5.1"],
      "artifacts": ["control-panel/ui/src/pages/OverviewPage.vue", "control-panel/ui/src/components/DaemonForm.vue", "control-panel/ui/src/components/ModelsTab.vue", "control-panel/ui/src/services/api.ts", "control-panel/ui/src/i18n/locales/ru.json", "control-panel/ui/src/i18n/locales/en.json"],
      "verify": "форма add (engine/model/port/name/settings), кнопка удаления инстанса, редактор per-instance настроек, удаление модели с dry-run preview + подтверждением; ru/en равны по ключам; vue-tsc + build зелёные",
      "evidence": "api.ts: addDaemon(POST /api/daemons), editDaemon(PATCH), removeDaemon(DELETE), deleteModel(id,{force,dryRun}). DaemonForm.vue (new): диалог add (engine-select whisper/vosk, model-select из models по modelName+эвристике движка, port авто/ручной, name/language, expansion per-instance settings из schema.wsDaemon target=daemon — toggle/number) и edit (model/port/settings, доп.-к-существующим). ModelsTab: кнопка delete (для скачанных) → openDelete делает dryRun → диалог с files/referencing_instances/external_dir → confirm делает force. OverviewPage Services: '+ Добавить' → openAdd; per-ws-daemon (kind==='ws-daemon') кнопки tune(edit)/delete(remove с window.confirm); загрузка models+schema (daemonFields=wsDaemon.filter target=daemon). server mapModels + modelName; тип ModelStatus + modelName. i18n: fleet.* + models.delete-группа, ru/en РАВНЫ (160==160). vue-tsc + tsc чисты; quasar build → server/public успешен; 18/18 server-тестов."
    },
    {
      "id": "DF5.3",
      "phase": "P5",
      "title": "pick-path/нативный диалог + полировка + docs",
      "status": "done",
      "dependsOn": ["DF5.2"],
      "artifacts": ["control-panel/README.md", "EchoRecorder/docs/ARCHITECTURE.md", "echoctl/README.md"],
      "verify": "нативный выбор пути сохранён; README echoctl (командный интерфейс); ARCHITECTURE обновлён (echoctl-движок + Bun-прокси); краткий how-to",
      "evidence": "pick-path (directory-picker.ts, /api/pick-path) сохранён без изменений в прокси. echoctl/README.md (new): что это (единственный писатель config + движок флота), build/smoke-скрипты, командный интерфейс + флаги (--json/--config/--manifest/--timeout), коды возврата, что управляет (инстансы/lifecycle/модели/config), scope-граница (оркестратор+streaming-vosk вне echoctl). control-panel/README.md: движок echoctl (обёртка), + CRUD инстансов/удаление моделей в 'What it does', обновлён API-список (POST/PATCH/DELETE daemons, DELETE models, pick-path), сокет-ограничение сужено до скрипт-запуска. ARCHITECTURE.md §6 (вложенный репо EchoRecorder — коммитится отдельно): движок echoctl + Bun-прокси, CRUD/lifecycle/модели через echoctl, фикс утечки :3001. Завершает P5."
    },
    {
      "id": "DF6.1",
      "phase": "P6",
      "title": "Оркестратор: динамический pickup add/remove инстансов (hot-reload reconcile)",
      "status": "todo",
      "dependsOn": ["DF1.1", "DF1.2"],
      "artifacts": ["orchestrator/src/scheduler.ts", "orchestrator/src/index.ts"],
      "verify": "добавленный через echoctl инстанс появляется в роутинге без рестарта оркестратора; удалённый — исчезает; при необходимости правка reconcile add/remove ws_daemons",
      "evidence": ""
    },
    {
      "id": "DF6.2",
      "phase": "P6",
      "title": "E2E: 2-я whisper-модель на новом порту → job; удаление модели refuse→force; фикс сокета",
      "status": "todo",
      "dependsOn": ["DF5.3", "DF4.1", "DF6.1"],
      "artifacts": ["echoctl/spec/daemon-fleet-progress.json"],
      "verify": "через UI: add 2-й whisper-модели на свободном порту → запуск → routing job → готовый результат; delete используемой модели отвергнут, с --force каскадит; подтверждено, что :3001 больше не удерживается демонами",
      "evidence": ""
    }
  ]
}
